/**
 * Shared Svelte 5 runes state for the benchmark app.
 * Compiled by the svelte plugin (runes mode) in rolldown.config.ts.
 */
/* eslint-disable no-undef -- $state is a Svelte 5 rune, compiled by the svelte plugin, not a runtime global. */
export const state = $state({
  rows: [],
  selectedId: null,
});
